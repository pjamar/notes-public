# notes-public
A set of notes-blog posts (with help of AI) so I can check them at a later time.

## MkDocs

Local setup:

```bash
python3 -m pip install -r requirements.txt
mkdocs serve
```

Build the static site with:

```bash
mkdocs build
```
